// 1. Default Parameters (การกำหนดค่าเริ่มต้น)

// ท่าที่ 1: ระบบทักทาย (ป้องกันค่าว่าง)
function welcomeUser(name = "คุณผู้ใช้งาน", role = "member") {
  console.log(`ยินดีต้อนรับ ${name} ในฐานะ ${role}`);
}

// welcomeUser("สมชาย", "admin"); // ผลลัพธ์: "ยินดีต้อนรับ สมชาย ในฐานะ admin"
// welcomeUser("สมศรี"); // ผลลัพธ์: "ยินดีต้อนรับ สมศรี ในฐานะ member" (ใช้ค่าเริ่มต้นของ role)
// welcomeUser(); // ผลลัพธ์: "ยินดีต้อนรับ คุณผู้ใช้งาน ในฐานะ member" (ใช้ค่าเริ่มต้นทั้งคู่)

// ท่าที่ 2: ตั้งค่า Object พื้นฐาน (นิยมมากในพวก Config)
// กำหนดค่าเริ่มต้นให้กับ Option ต่าง ๆ ในการเชื่อมต่อฐานข้อมูล
function connectDatabase(options = { host: "localhost", port: 3306 }) {
  console.log(`กำลังเชื่อมต่อกับ ${options.host}:${options.port}`);
}

connectDatabase(); // ผลลัพธ์: "กำลังเชื่อมต่อกับ localhost:3306"

// ท่าที่ 3: ใช้ฟังก์ชันอื่นมาเป็นค่าเริ่มต้น (Dynamic Default)
function generateId() {
  return "ID-" + Math.floor(Math.random() * 1000);
}

// ถ้าคนสร้างทาสก์ไม่ส่ง id มา ระบบจะรันฟังก์ชัน generateId() ให้เองอัตโนมัติ
function createTask(title, id = generateId()) {
  console.log(`สร้างทาสก์: ${title} (รหัส: ${id})`);
}

createTask("ซักผ้า"); // ผลลัพธ์: "สร้างทาสก์: ซักผ้า (รหัส: ID-542)"

// 2. Rest Parameters (ตัวช่วยรับพารามิเตอร์แบบไม่จำกัด)

// ท่าที่ 1: ฟังก์ชันคำนวณที่รับค่ากี่ตัวก็ได้
// ...numbers จะรวบรวมเลขทุกตัวที่ส่งเข้ามาแปลงเป็น Array ชื่อ numbers
function sum(...numbers) {
  // พอเป็น Array แล้ว ก็ใช้คำสั่งของ Array อย่าง reduce ได้สบายมาก
  return numbers.reduce((total, num) => total + num, 0);
}

console.log(sum(1, 2, 3)); // ผลลัพธ์: 6
console.log(sum(10, 20, 30, 40)); // ผลลัพธ์: 100

// ท่าที่ 2: ล็อคตัวแปรหลัก ส่วนที่เหลือรวบใส่ Rest
function โชว์เหรียญรางวัล(ที่หนึ่ง, ที่สอง, ...คนอื่นที่ได้รับชมเชย) {
  console.log(`🥇 เหรียญทอง: ${ที่หนึ่ง}`);
  console.log(`🥈 เหรียญเงิน: ${ที่สอง}`);
  console.log(`🎗️ รางวัลชมเชย: `, คนอื่นที่ได้รับชมเชย); // ตัวแปรนี้จะเป็น Array เก็บคนที่เหลือ
}

โชว์เหรียญรางวัล("เจมส์", "เนย", "อาร์ม", "ตั้ม", "พลอย");
/* 
ผลลัพธ์:
🥇 เหรียญทอง: เจมส์
🥈 เหรียญเงิน: เนย
🎗️ รางวัลชมเชย: ['อาร์ม', 'ตั้ม', 'พลอย']
*/

/*
ข้อควรระวังและการใช้ร่วมกัน (มิกซ์แอนด์แมทช์)
เราสามารถนำทั้ง Default และ Rest มาเขียนอยู่ในฟังก์ชันเดียวกันได้เลยครับ 
แต่ต้องเรียงลำดับให้ถูกต้อง คือ พารามิเตอร์ปกติ -> ตัวที่มี Default -> Rest Parameter (ตัวสุดท้าย)
*/
function สั่งอาหาร(เมนูหลัก, ขนม = "เค้กส้ม", ...เครื่องดื่ม) {
  console.log(`จานหลัก: ${เมนูหลัก}`);
  console.log(`ของหวาน: ${ขนม}`);
  console.log(`น้ำดื่ม: ${เครื่องดื่ม.join(", ")}`);
}

สั่งอาหาร("สเต็กเนื้อ", undefined, "โค้ก", "ชามะนาว", "น้ำเปล่า");
/*
ผลลัพธ์:
จานหลัก: สเต็กเนื้อ
ของหวาน: เค้กส้ม (ส่ง undefined มา มันเลยดึงเดฟอลต์มาใช้)
น้ำดื่ม: โค้ก, ชามะนาว, น้ำเปล่า
*/
